import 'package:flutter/material.dart';

class PenjualPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Penjual'),
      ),
      body: Center(
        child: Text('Ini adalah halaman penjual'),
      ),
    );
  }
}
